// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"balanciz/ent/searchusagestat"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
)

// SearchUsageStat is the model entity for the SearchUsageStat schema.
type SearchUsageStat struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// CompanyID holds the value of the "company_id" field.
	CompanyID uint `json:"company_id,omitempty"`
	// EntityType holds the value of the "entity_type" field.
	EntityType string `json:"entity_type,omitempty"`
	// EntityID holds the value of the "entity_id" field.
	EntityID uint `json:"entity_id,omitempty"`
	// Total click-through count from the global search dropdown.
	ClickCount int `json:"click_count,omitempty"`
	// Wall-clock of the most recent click — recency tie-breaker for the ranker.
	LastClickedAt time.Time `json:"last_clicked_at,omitempty"`
	selectValues  sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SearchUsageStat) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case searchusagestat.FieldID, searchusagestat.FieldCompanyID, searchusagestat.FieldEntityID, searchusagestat.FieldClickCount:
			values[i] = new(sql.NullInt64)
		case searchusagestat.FieldEntityType:
			values[i] = new(sql.NullString)
		case searchusagestat.FieldLastClickedAt:
			values[i] = new(sql.NullTime)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SearchUsageStat fields.
func (sus *SearchUsageStat) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case searchusagestat.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			sus.ID = int(value.Int64)
		case searchusagestat.FieldCompanyID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field company_id", values[i])
			} else if value.Valid {
				sus.CompanyID = uint(value.Int64)
			}
		case searchusagestat.FieldEntityType:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field entity_type", values[i])
			} else if value.Valid {
				sus.EntityType = value.String
			}
		case searchusagestat.FieldEntityID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field entity_id", values[i])
			} else if value.Valid {
				sus.EntityID = uint(value.Int64)
			}
		case searchusagestat.FieldClickCount:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field click_count", values[i])
			} else if value.Valid {
				sus.ClickCount = int(value.Int64)
			}
		case searchusagestat.FieldLastClickedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field last_clicked_at", values[i])
			} else if value.Valid {
				sus.LastClickedAt = value.Time
			}
		default:
			sus.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SearchUsageStat.
// This includes values selected through modifiers, order, etc.
func (sus *SearchUsageStat) Value(name string) (ent.Value, error) {
	return sus.selectValues.Get(name)
}

// Update returns a builder for updating this SearchUsageStat.
// Note that you need to call SearchUsageStat.Unwrap() before calling this method if this SearchUsageStat
// was returned from a transaction, and the transaction was committed or rolled back.
func (sus *SearchUsageStat) Update() *SearchUsageStatUpdateOne {
	return NewSearchUsageStatClient(sus.config).UpdateOne(sus)
}

// Unwrap unwraps the SearchUsageStat entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (sus *SearchUsageStat) Unwrap() *SearchUsageStat {
	_tx, ok := sus.config.driver.(*txDriver)
	if !ok {
		panic("ent: SearchUsageStat is not a transactional entity")
	}
	sus.config.driver = _tx.drv
	return sus
}

// String implements the fmt.Stringer.
func (sus *SearchUsageStat) String() string {
	var builder strings.Builder
	builder.WriteString("SearchUsageStat(")
	builder.WriteString(fmt.Sprintf("id=%v, ", sus.ID))
	builder.WriteString("company_id=")
	builder.WriteString(fmt.Sprintf("%v", sus.CompanyID))
	builder.WriteString(", ")
	builder.WriteString("entity_type=")
	builder.WriteString(sus.EntityType)
	builder.WriteString(", ")
	builder.WriteString("entity_id=")
	builder.WriteString(fmt.Sprintf("%v", sus.EntityID))
	builder.WriteString(", ")
	builder.WriteString("click_count=")
	builder.WriteString(fmt.Sprintf("%v", sus.ClickCount))
	builder.WriteString(", ")
	builder.WriteString("last_clicked_at=")
	builder.WriteString(sus.LastClickedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SearchUsageStats is a parsable slice of SearchUsageStat.
type SearchUsageStats []*SearchUsageStat
