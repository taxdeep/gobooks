// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"balanciz/ent/predicate"
	"balanciz/ent/searchrecentquery"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SearchRecentQueryQuery is the builder for querying SearchRecentQuery entities.
type SearchRecentQueryQuery struct {
	config
	ctx        *QueryContext
	order      []searchrecentquery.OrderOption
	inters     []Interceptor
	predicates []predicate.SearchRecentQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SearchRecentQueryQuery builder.
func (srqq *SearchRecentQueryQuery) Where(ps ...predicate.SearchRecentQuery) *SearchRecentQueryQuery {
	srqq.predicates = append(srqq.predicates, ps...)
	return srqq
}

// Limit the number of records to be returned by this query.
func (srqq *SearchRecentQueryQuery) Limit(limit int) *SearchRecentQueryQuery {
	srqq.ctx.Limit = &limit
	return srqq
}

// Offset to start from.
func (srqq *SearchRecentQueryQuery) Offset(offset int) *SearchRecentQueryQuery {
	srqq.ctx.Offset = &offset
	return srqq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (srqq *SearchRecentQueryQuery) Unique(unique bool) *SearchRecentQueryQuery {
	srqq.ctx.Unique = &unique
	return srqq
}

// Order specifies how the records should be ordered.
func (srqq *SearchRecentQueryQuery) Order(o ...searchrecentquery.OrderOption) *SearchRecentQueryQuery {
	srqq.order = append(srqq.order, o...)
	return srqq
}

// First returns the first SearchRecentQuery entity from the query.
// Returns a *NotFoundError when no SearchRecentQuery was found.
func (srqq *SearchRecentQueryQuery) First(ctx context.Context) (*SearchRecentQuery, error) {
	nodes, err := srqq.Limit(1).All(setContextOp(ctx, srqq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{searchrecentquery.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (srqq *SearchRecentQueryQuery) FirstX(ctx context.Context) *SearchRecentQuery {
	node, err := srqq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SearchRecentQuery ID from the query.
// Returns a *NotFoundError when no SearchRecentQuery ID was found.
func (srqq *SearchRecentQueryQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = srqq.Limit(1).IDs(setContextOp(ctx, srqq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{searchrecentquery.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (srqq *SearchRecentQueryQuery) FirstIDX(ctx context.Context) int {
	id, err := srqq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SearchRecentQuery entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SearchRecentQuery entity is found.
// Returns a *NotFoundError when no SearchRecentQuery entities are found.
func (srqq *SearchRecentQueryQuery) Only(ctx context.Context) (*SearchRecentQuery, error) {
	nodes, err := srqq.Limit(2).All(setContextOp(ctx, srqq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{searchrecentquery.Label}
	default:
		return nil, &NotSingularError{searchrecentquery.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (srqq *SearchRecentQueryQuery) OnlyX(ctx context.Context) *SearchRecentQuery {
	node, err := srqq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SearchRecentQuery ID in the query.
// Returns a *NotSingularError when more than one SearchRecentQuery ID is found.
// Returns a *NotFoundError when no entities are found.
func (srqq *SearchRecentQueryQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = srqq.Limit(2).IDs(setContextOp(ctx, srqq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{searchrecentquery.Label}
	default:
		err = &NotSingularError{searchrecentquery.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (srqq *SearchRecentQueryQuery) OnlyIDX(ctx context.Context) int {
	id, err := srqq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SearchRecentQueries.
func (srqq *SearchRecentQueryQuery) All(ctx context.Context) ([]*SearchRecentQuery, error) {
	ctx = setContextOp(ctx, srqq.ctx, ent.OpQueryAll)
	if err := srqq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SearchRecentQuery, *SearchRecentQueryQuery]()
	return withInterceptors[[]*SearchRecentQuery](ctx, srqq, qr, srqq.inters)
}

// AllX is like All, but panics if an error occurs.
func (srqq *SearchRecentQueryQuery) AllX(ctx context.Context) []*SearchRecentQuery {
	nodes, err := srqq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SearchRecentQuery IDs.
func (srqq *SearchRecentQueryQuery) IDs(ctx context.Context) (ids []int, err error) {
	if srqq.ctx.Unique == nil && srqq.path != nil {
		srqq.Unique(true)
	}
	ctx = setContextOp(ctx, srqq.ctx, ent.OpQueryIDs)
	if err = srqq.Select(searchrecentquery.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (srqq *SearchRecentQueryQuery) IDsX(ctx context.Context) []int {
	ids, err := srqq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (srqq *SearchRecentQueryQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, srqq.ctx, ent.OpQueryCount)
	if err := srqq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, srqq, querierCount[*SearchRecentQueryQuery](), srqq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (srqq *SearchRecentQueryQuery) CountX(ctx context.Context) int {
	count, err := srqq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (srqq *SearchRecentQueryQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, srqq.ctx, ent.OpQueryExist)
	switch _, err := srqq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (srqq *SearchRecentQueryQuery) ExistX(ctx context.Context) bool {
	exist, err := srqq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SearchRecentQueryQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (srqq *SearchRecentQueryQuery) Clone() *SearchRecentQueryQuery {
	if srqq == nil {
		return nil
	}
	return &SearchRecentQueryQuery{
		config:     srqq.config,
		ctx:        srqq.ctx.Clone(),
		order:      append([]searchrecentquery.OrderOption{}, srqq.order...),
		inters:     append([]Interceptor{}, srqq.inters...),
		predicates: append([]predicate.SearchRecentQuery{}, srqq.predicates...),
		// clone intermediate query.
		sql:  srqq.sql.Clone(),
		path: srqq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SearchRecentQuery.Query().
//		GroupBy(searchrecentquery.FieldUserID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (srqq *SearchRecentQueryQuery) GroupBy(field string, fields ...string) *SearchRecentQueryGroupBy {
	srqq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SearchRecentQueryGroupBy{build: srqq}
	grbuild.flds = &srqq.ctx.Fields
	grbuild.label = searchrecentquery.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		UserID uuid.UUID `json:"user_id,omitempty"`
//	}
//
//	client.SearchRecentQuery.Query().
//		Select(searchrecentquery.FieldUserID).
//		Scan(ctx, &v)
func (srqq *SearchRecentQueryQuery) Select(fields ...string) *SearchRecentQuerySelect {
	srqq.ctx.Fields = append(srqq.ctx.Fields, fields...)
	sbuild := &SearchRecentQuerySelect{SearchRecentQueryQuery: srqq}
	sbuild.label = searchrecentquery.Label
	sbuild.flds, sbuild.scan = &srqq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SearchRecentQuerySelect configured with the given aggregations.
func (srqq *SearchRecentQueryQuery) Aggregate(fns ...AggregateFunc) *SearchRecentQuerySelect {
	return srqq.Select().Aggregate(fns...)
}

func (srqq *SearchRecentQueryQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range srqq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, srqq); err != nil {
				return err
			}
		}
	}
	for _, f := range srqq.ctx.Fields {
		if !searchrecentquery.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if srqq.path != nil {
		prev, err := srqq.path(ctx)
		if err != nil {
			return err
		}
		srqq.sql = prev
	}
	return nil
}

func (srqq *SearchRecentQueryQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SearchRecentQuery, error) {
	var (
		nodes = []*SearchRecentQuery{}
		_spec = srqq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SearchRecentQuery).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SearchRecentQuery{config: srqq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, srqq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (srqq *SearchRecentQueryQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := srqq.querySpec()
	_spec.Node.Columns = srqq.ctx.Fields
	if len(srqq.ctx.Fields) > 0 {
		_spec.Unique = srqq.ctx.Unique != nil && *srqq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, srqq.driver, _spec)
}

func (srqq *SearchRecentQueryQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(searchrecentquery.Table, searchrecentquery.Columns, sqlgraph.NewFieldSpec(searchrecentquery.FieldID, field.TypeInt))
	_spec.From = srqq.sql
	if unique := srqq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if srqq.path != nil {
		_spec.Unique = true
	}
	if fields := srqq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, searchrecentquery.FieldID)
		for i := range fields {
			if fields[i] != searchrecentquery.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := srqq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := srqq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := srqq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := srqq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (srqq *SearchRecentQueryQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(srqq.driver.Dialect())
	t1 := builder.Table(searchrecentquery.Table)
	columns := srqq.ctx.Fields
	if len(columns) == 0 {
		columns = searchrecentquery.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if srqq.sql != nil {
		selector = srqq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if srqq.ctx.Unique != nil && *srqq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range srqq.predicates {
		p(selector)
	}
	for _, p := range srqq.order {
		p(selector)
	}
	if offset := srqq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := srqq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SearchRecentQueryGroupBy is the group-by builder for SearchRecentQuery entities.
type SearchRecentQueryGroupBy struct {
	selector
	build *SearchRecentQueryQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (srqgb *SearchRecentQueryGroupBy) Aggregate(fns ...AggregateFunc) *SearchRecentQueryGroupBy {
	srqgb.fns = append(srqgb.fns, fns...)
	return srqgb
}

// Scan applies the selector query and scans the result into the given value.
func (srqgb *SearchRecentQueryGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srqgb.build.ctx, ent.OpQueryGroupBy)
	if err := srqgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SearchRecentQueryQuery, *SearchRecentQueryGroupBy](ctx, srqgb.build, srqgb, srqgb.build.inters, v)
}

func (srqgb *SearchRecentQueryGroupBy) sqlScan(ctx context.Context, root *SearchRecentQueryQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(srqgb.fns))
	for _, fn := range srqgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*srqgb.flds)+len(srqgb.fns))
		for _, f := range *srqgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*srqgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srqgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SearchRecentQuerySelect is the builder for selecting fields of SearchRecentQuery entities.
type SearchRecentQuerySelect struct {
	*SearchRecentQueryQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (srqs *SearchRecentQuerySelect) Aggregate(fns ...AggregateFunc) *SearchRecentQuerySelect {
	srqs.fns = append(srqs.fns, fns...)
	return srqs
}

// Scan applies the selector query and scans the result into the given value.
func (srqs *SearchRecentQuerySelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, srqs.ctx, ent.OpQuerySelect)
	if err := srqs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SearchRecentQueryQuery, *SearchRecentQuerySelect](ctx, srqs.SearchRecentQueryQuery, srqs, srqs.inters, v)
}

func (srqs *SearchRecentQuerySelect) sqlScan(ctx context.Context, root *SearchRecentQueryQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(srqs.fns))
	for _, fn := range srqs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*srqs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := srqs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
