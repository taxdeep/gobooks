// Code generated by ent, DO NOT EDIT.

package ent

import (
	"fmt"
	"balanciz/ent/searchrecentquery"
	"strings"
	"time"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"github.com/google/uuid"
)

// SearchRecentQuery is the model entity for the SearchRecentQuery schema.
type SearchRecentQuery struct {
	config `json:"-"`
	// ID of the ent.
	ID int `json:"id,omitempty"`
	// User who issued the query.
	UserID uuid.UUID `json:"user_id,omitempty"`
	// Active company at query time.
	CompanyID uint `json:"company_id,omitempty"`
	// Raw query string as the user typed it (preserves case for replay).
	Query string `json:"query,omitempty"`
	// Wall-clock at time of query.
	QueriedAt    time.Time `json:"queried_at,omitempty"`
	selectValues sql.SelectValues
}

// scanValues returns the types for scanning values from sql.Rows.
func (*SearchRecentQuery) scanValues(columns []string) ([]any, error) {
	values := make([]any, len(columns))
	for i := range columns {
		switch columns[i] {
		case searchrecentquery.FieldID, searchrecentquery.FieldCompanyID:
			values[i] = new(sql.NullInt64)
		case searchrecentquery.FieldQuery:
			values[i] = new(sql.NullString)
		case searchrecentquery.FieldQueriedAt:
			values[i] = new(sql.NullTime)
		case searchrecentquery.FieldUserID:
			values[i] = new(uuid.UUID)
		default:
			values[i] = new(sql.UnknownType)
		}
	}
	return values, nil
}

// assignValues assigns the values that were returned from sql.Rows (after scanning)
// to the SearchRecentQuery fields.
func (srq *SearchRecentQuery) assignValues(columns []string, values []any) error {
	if m, n := len(values), len(columns); m < n {
		return fmt.Errorf("mismatch number of scan values: %d != %d", m, n)
	}
	for i := range columns {
		switch columns[i] {
		case searchrecentquery.FieldID:
			value, ok := values[i].(*sql.NullInt64)
			if !ok {
				return fmt.Errorf("unexpected type %T for field id", value)
			}
			srq.ID = int(value.Int64)
		case searchrecentquery.FieldUserID:
			if value, ok := values[i].(*uuid.UUID); !ok {
				return fmt.Errorf("unexpected type %T for field user_id", values[i])
			} else if value != nil {
				srq.UserID = *value
			}
		case searchrecentquery.FieldCompanyID:
			if value, ok := values[i].(*sql.NullInt64); !ok {
				return fmt.Errorf("unexpected type %T for field company_id", values[i])
			} else if value.Valid {
				srq.CompanyID = uint(value.Int64)
			}
		case searchrecentquery.FieldQuery:
			if value, ok := values[i].(*sql.NullString); !ok {
				return fmt.Errorf("unexpected type %T for field query", values[i])
			} else if value.Valid {
				srq.Query = value.String
			}
		case searchrecentquery.FieldQueriedAt:
			if value, ok := values[i].(*sql.NullTime); !ok {
				return fmt.Errorf("unexpected type %T for field queried_at", values[i])
			} else if value.Valid {
				srq.QueriedAt = value.Time
			}
		default:
			srq.selectValues.Set(columns[i], values[i])
		}
	}
	return nil
}

// Value returns the ent.Value that was dynamically selected and assigned to the SearchRecentQuery.
// This includes values selected through modifiers, order, etc.
func (srq *SearchRecentQuery) Value(name string) (ent.Value, error) {
	return srq.selectValues.Get(name)
}

// Update returns a builder for updating this SearchRecentQuery.
// Note that you need to call SearchRecentQuery.Unwrap() before calling this method if this SearchRecentQuery
// was returned from a transaction, and the transaction was committed or rolled back.
func (srq *SearchRecentQuery) Update() *SearchRecentQueryUpdateOne {
	return NewSearchRecentQueryClient(srq.config).UpdateOne(srq)
}

// Unwrap unwraps the SearchRecentQuery entity that was returned from a transaction after it was closed,
// so that all future queries will be executed through the driver which created the transaction.
func (srq *SearchRecentQuery) Unwrap() *SearchRecentQuery {
	_tx, ok := srq.config.driver.(*txDriver)
	if !ok {
		panic("ent: SearchRecentQuery is not a transactional entity")
	}
	srq.config.driver = _tx.drv
	return srq
}

// String implements the fmt.Stringer.
func (srq *SearchRecentQuery) String() string {
	var builder strings.Builder
	builder.WriteString("SearchRecentQuery(")
	builder.WriteString(fmt.Sprintf("id=%v, ", srq.ID))
	builder.WriteString("user_id=")
	builder.WriteString(fmt.Sprintf("%v", srq.UserID))
	builder.WriteString(", ")
	builder.WriteString("company_id=")
	builder.WriteString(fmt.Sprintf("%v", srq.CompanyID))
	builder.WriteString(", ")
	builder.WriteString("query=")
	builder.WriteString(srq.Query)
	builder.WriteString(", ")
	builder.WriteString("queried_at=")
	builder.WriteString(srq.QueriedAt.Format(time.ANSIC))
	builder.WriteByte(')')
	return builder.String()
}

// SearchRecentQueries is a parsable slice of SearchRecentQuery.
type SearchRecentQueries []*SearchRecentQuery
