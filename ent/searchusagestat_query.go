// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"balanciz/ent/predicate"
	"balanciz/ent/searchusagestat"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// SearchUsageStatQuery is the builder for querying SearchUsageStat entities.
type SearchUsageStatQuery struct {
	config
	ctx        *QueryContext
	order      []searchusagestat.OrderOption
	inters     []Interceptor
	predicates []predicate.SearchUsageStat
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the SearchUsageStatQuery builder.
func (susq *SearchUsageStatQuery) Where(ps ...predicate.SearchUsageStat) *SearchUsageStatQuery {
	susq.predicates = append(susq.predicates, ps...)
	return susq
}

// Limit the number of records to be returned by this query.
func (susq *SearchUsageStatQuery) Limit(limit int) *SearchUsageStatQuery {
	susq.ctx.Limit = &limit
	return susq
}

// Offset to start from.
func (susq *SearchUsageStatQuery) Offset(offset int) *SearchUsageStatQuery {
	susq.ctx.Offset = &offset
	return susq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (susq *SearchUsageStatQuery) Unique(unique bool) *SearchUsageStatQuery {
	susq.ctx.Unique = &unique
	return susq
}

// Order specifies how the records should be ordered.
func (susq *SearchUsageStatQuery) Order(o ...searchusagestat.OrderOption) *SearchUsageStatQuery {
	susq.order = append(susq.order, o...)
	return susq
}

// First returns the first SearchUsageStat entity from the query.
// Returns a *NotFoundError when no SearchUsageStat was found.
func (susq *SearchUsageStatQuery) First(ctx context.Context) (*SearchUsageStat, error) {
	nodes, err := susq.Limit(1).All(setContextOp(ctx, susq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{searchusagestat.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (susq *SearchUsageStatQuery) FirstX(ctx context.Context) *SearchUsageStat {
	node, err := susq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first SearchUsageStat ID from the query.
// Returns a *NotFoundError when no SearchUsageStat ID was found.
func (susq *SearchUsageStatQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = susq.Limit(1).IDs(setContextOp(ctx, susq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{searchusagestat.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (susq *SearchUsageStatQuery) FirstIDX(ctx context.Context) int {
	id, err := susq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single SearchUsageStat entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one SearchUsageStat entity is found.
// Returns a *NotFoundError when no SearchUsageStat entities are found.
func (susq *SearchUsageStatQuery) Only(ctx context.Context) (*SearchUsageStat, error) {
	nodes, err := susq.Limit(2).All(setContextOp(ctx, susq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{searchusagestat.Label}
	default:
		return nil, &NotSingularError{searchusagestat.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (susq *SearchUsageStatQuery) OnlyX(ctx context.Context) *SearchUsageStat {
	node, err := susq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only SearchUsageStat ID in the query.
// Returns a *NotSingularError when more than one SearchUsageStat ID is found.
// Returns a *NotFoundError when no entities are found.
func (susq *SearchUsageStatQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = susq.Limit(2).IDs(setContextOp(ctx, susq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{searchusagestat.Label}
	default:
		err = &NotSingularError{searchusagestat.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (susq *SearchUsageStatQuery) OnlyIDX(ctx context.Context) int {
	id, err := susq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of SearchUsageStats.
func (susq *SearchUsageStatQuery) All(ctx context.Context) ([]*SearchUsageStat, error) {
	ctx = setContextOp(ctx, susq.ctx, ent.OpQueryAll)
	if err := susq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*SearchUsageStat, *SearchUsageStatQuery]()
	return withInterceptors[[]*SearchUsageStat](ctx, susq, qr, susq.inters)
}

// AllX is like All, but panics if an error occurs.
func (susq *SearchUsageStatQuery) AllX(ctx context.Context) []*SearchUsageStat {
	nodes, err := susq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of SearchUsageStat IDs.
func (susq *SearchUsageStatQuery) IDs(ctx context.Context) (ids []int, err error) {
	if susq.ctx.Unique == nil && susq.path != nil {
		susq.Unique(true)
	}
	ctx = setContextOp(ctx, susq.ctx, ent.OpQueryIDs)
	if err = susq.Select(searchusagestat.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (susq *SearchUsageStatQuery) IDsX(ctx context.Context) []int {
	ids, err := susq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (susq *SearchUsageStatQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, susq.ctx, ent.OpQueryCount)
	if err := susq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, susq, querierCount[*SearchUsageStatQuery](), susq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (susq *SearchUsageStatQuery) CountX(ctx context.Context) int {
	count, err := susq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (susq *SearchUsageStatQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, susq.ctx, ent.OpQueryExist)
	switch _, err := susq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (susq *SearchUsageStatQuery) ExistX(ctx context.Context) bool {
	exist, err := susq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the SearchUsageStatQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (susq *SearchUsageStatQuery) Clone() *SearchUsageStatQuery {
	if susq == nil {
		return nil
	}
	return &SearchUsageStatQuery{
		config:     susq.config,
		ctx:        susq.ctx.Clone(),
		order:      append([]searchusagestat.OrderOption{}, susq.order...),
		inters:     append([]Interceptor{}, susq.inters...),
		predicates: append([]predicate.SearchUsageStat{}, susq.predicates...),
		// clone intermediate query.
		sql:  susq.sql.Clone(),
		path: susq.path,
	}
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		CompanyID uint `json:"company_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.SearchUsageStat.Query().
//		GroupBy(searchusagestat.FieldCompanyID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (susq *SearchUsageStatQuery) GroupBy(field string, fields ...string) *SearchUsageStatGroupBy {
	susq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &SearchUsageStatGroupBy{build: susq}
	grbuild.flds = &susq.ctx.Fields
	grbuild.label = searchusagestat.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		CompanyID uint `json:"company_id,omitempty"`
//	}
//
//	client.SearchUsageStat.Query().
//		Select(searchusagestat.FieldCompanyID).
//		Scan(ctx, &v)
func (susq *SearchUsageStatQuery) Select(fields ...string) *SearchUsageStatSelect {
	susq.ctx.Fields = append(susq.ctx.Fields, fields...)
	sbuild := &SearchUsageStatSelect{SearchUsageStatQuery: susq}
	sbuild.label = searchusagestat.Label
	sbuild.flds, sbuild.scan = &susq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a SearchUsageStatSelect configured with the given aggregations.
func (susq *SearchUsageStatQuery) Aggregate(fns ...AggregateFunc) *SearchUsageStatSelect {
	return susq.Select().Aggregate(fns...)
}

func (susq *SearchUsageStatQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range susq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, susq); err != nil {
				return err
			}
		}
	}
	for _, f := range susq.ctx.Fields {
		if !searchusagestat.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if susq.path != nil {
		prev, err := susq.path(ctx)
		if err != nil {
			return err
		}
		susq.sql = prev
	}
	return nil
}

func (susq *SearchUsageStatQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*SearchUsageStat, error) {
	var (
		nodes = []*SearchUsageStat{}
		_spec = susq.querySpec()
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*SearchUsageStat).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &SearchUsageStat{config: susq.config}
		nodes = append(nodes, node)
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, susq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	return nodes, nil
}

func (susq *SearchUsageStatQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := susq.querySpec()
	_spec.Node.Columns = susq.ctx.Fields
	if len(susq.ctx.Fields) > 0 {
		_spec.Unique = susq.ctx.Unique != nil && *susq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, susq.driver, _spec)
}

func (susq *SearchUsageStatQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(searchusagestat.Table, searchusagestat.Columns, sqlgraph.NewFieldSpec(searchusagestat.FieldID, field.TypeInt))
	_spec.From = susq.sql
	if unique := susq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if susq.path != nil {
		_spec.Unique = true
	}
	if fields := susq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, searchusagestat.FieldID)
		for i := range fields {
			if fields[i] != searchusagestat.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := susq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := susq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := susq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := susq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (susq *SearchUsageStatQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(susq.driver.Dialect())
	t1 := builder.Table(searchusagestat.Table)
	columns := susq.ctx.Fields
	if len(columns) == 0 {
		columns = searchusagestat.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if susq.sql != nil {
		selector = susq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if susq.ctx.Unique != nil && *susq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range susq.predicates {
		p(selector)
	}
	for _, p := range susq.order {
		p(selector)
	}
	if offset := susq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := susq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// SearchUsageStatGroupBy is the group-by builder for SearchUsageStat entities.
type SearchUsageStatGroupBy struct {
	selector
	build *SearchUsageStatQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (susgb *SearchUsageStatGroupBy) Aggregate(fns ...AggregateFunc) *SearchUsageStatGroupBy {
	susgb.fns = append(susgb.fns, fns...)
	return susgb
}

// Scan applies the selector query and scans the result into the given value.
func (susgb *SearchUsageStatGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, susgb.build.ctx, ent.OpQueryGroupBy)
	if err := susgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SearchUsageStatQuery, *SearchUsageStatGroupBy](ctx, susgb.build, susgb, susgb.build.inters, v)
}

func (susgb *SearchUsageStatGroupBy) sqlScan(ctx context.Context, root *SearchUsageStatQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(susgb.fns))
	for _, fn := range susgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*susgb.flds)+len(susgb.fns))
		for _, f := range *susgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*susgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := susgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// SearchUsageStatSelect is the builder for selecting fields of SearchUsageStat entities.
type SearchUsageStatSelect struct {
	*SearchUsageStatQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (suss *SearchUsageStatSelect) Aggregate(fns ...AggregateFunc) *SearchUsageStatSelect {
	suss.fns = append(suss.fns, fns...)
	return suss
}

// Scan applies the selector query and scans the result into the given value.
func (suss *SearchUsageStatSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, suss.ctx, ent.OpQuerySelect)
	if err := suss.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*SearchUsageStatQuery, *SearchUsageStatSelect](ctx, suss.SearchUsageStatQuery, suss, suss.inters, v)
}

func (suss *SearchUsageStatSelect) sqlScan(ctx context.Context, root *SearchUsageStatQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(suss.fns))
	for _, fn := range suss.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*suss.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := suss.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
