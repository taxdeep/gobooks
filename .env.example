# 遵循project_guide.md

# Server
APP_ENV=dev
APP_ADDR=:6768

# Logging — DEBUG | INFO | WARN | ERROR (default: INFO)
LOG_LEVEL=INFO

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=balanciz
DB_PASSWORD=balanciz
DB_NAME=balanciz
DB_SSLMODE=disable

# AI integration (optional — leave blank to disable AI features)
AI_SECRET_KEY=
